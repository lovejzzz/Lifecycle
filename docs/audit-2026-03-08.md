# Lifecycle App Audit — 2026-03-08

**Stack:** Next.js 16, React 19, React Flow 12, Zustand 5, Framer Motion 12, Tailwind CSS 4
**Auditor:** Claude Code | **Round:** 91

## Grades

| Area | Grade | Notes |
|------|-------|-------|
| Architecture | **A-** | Agent-first, 5-layer personality, clean module separation. Store monolith (-) |
| Security | **B+** | Server-side-only API keys, input validation, env isolation. Missing rate limiting & HTML sanitization (-) |
| Performance | **B** | Efficient topoSort (Kahn's O(V+E)), lazy panels, ephemeral filtering. Store re-renders & no drag debouncing (-) |
| Code Quality | **A-** | Strong TypeScript, comprehensive types. 6 lint warnings (down from 23 over 3 rounds). Remaining are structural React patterns (-) |
| Error Handling | **B** | API route has retry/timeout/fallback. Empty catch blocks now use bare `catch` instead of unused vars (+). Missing React error boundaries (-) |
| State Management | **B+** | Zustand with 70+ actions, proper persistence filtering. Single 4,622-line store (-) |
| Accessibility | **C+** | Keyboard shortcuts, high contrast. Missing ARIA labels, focus traps, semantic HTML (-) |
| Testing | **C+** | Eval harness (60-test pool, model override) covers AI quality. 6 consecutive 100% scores. No unit tests for graph/intent/reflection (-) |

## What's Working Well

1. **Agent Intelligence**: 5-layer personality system (Temperament → Drive → Habit → Generation → Reflection) genuinely evolves agent behavior over time. Domain expertise tracking with decay is elegant.
2. **Eval Harness**: 60-test rotating pool covering 12+ industries, 4 task types, edge cases. Model override (`deepseek-chat` / `deepseek-reasoner`) enables cross-model comparison. 6 consecutive 100% scores across both models.
3. **API Route Robustness**: Non-CID JSON detection, node reordering (output always last), multi-provider support (DeepSeek, Anthropic, OpenRouter), retry on 429, timeout handling.
4. **Graph Utilities**: Clean extraction in Round 68. topoSort, edge inference, overlap resolution all well-bounded.
5. **Dual Agent Personalities**: Rowan (Soldier) and Poirot (Detective) produce consistently distinct, character-appropriate output across models.
6. **Lint Trajectory**: 23 → 14 → 9 → 6 warnings across 3 rounds. Remaining 6 are structural React patterns (set-state-in-effect, static-components, exhaustive-deps) requiring refactoring.

## Key Findings

### Security
- `src/app/api/cid/route.ts`: No rate limiting — vulnerable to API abuse (HIGH)
- `src/lib/graph.ts:158-176`: `markdownToHTML` escapes `<>` but doesn't use DOMPurify — XSS risk if LLM output is malicious (HIGH)
- `src/lib/types.ts:228`: Deprecated `apiKey` field still in NodeData interface — should be removed (LOW)

### Performance
- `src/store/useStore.ts`: 4,622 lines, 70+ methods — every action triggers subscriber re-renders (MEDIUM)
- `src/lib/graph.ts:100-130`: `resolveOverlap` called on every drag without debouncing (LOW)
- `src/lib/graph.ts:73-98`: `findFreePosition` tries 50 collision attempts — acceptable for <20 nodes, degrades for dense graphs (LOW)

### Code Quality
- Lint warnings: 23 → 6 across three rounds (fixed 17 total)
- Remaining 6 warnings are all structural React patterns, not simple variable fixes:
  - `NodeDetailPanel.tsx:359` — setState in effect (editing state reset)
  - `NodeDetailPanel.tsx:108,200,490` — static component creation during render
  - `TopBar.tsx:39,45` — setState in effect (mounted, hasUnread)
  - `Canvas.tsx:436` — exhaustive-deps missing deleteEdge, nodes
  - `Canvas.tsx:1124` — ref access during render
- `src/lib/reflection.ts:557-565`: `processAllReflections` and `detectPatterns` are stubs — legacy compat (LOW)

### Error Handling
- Empty catch blocks now use bare `catch` syntax (cleaner) — FIXED this round
- No React error boundary at Canvas or CID panel level — unhandled errors crash the entire app (MEDIUM)
- Edge label normalization silently maps unknown labels to 'drives' (LOW)

### Accessibility
- Icon-only buttons (undo, redo, export, lock) lack `aria-label` attributes throughout `TopBar.tsx` and `NodeDetailPanel.tsx` (MEDIUM)
- Dropdown menus lack `aria-expanded`, `aria-haspopup`, and focus trap (MEDIUM)
- Status changes animated with Framer Motion but no `aria-live` announcements (LOW)
- Section headers use `<span>` instead of semantic heading elements (LOW)

## Priority Actions

1. **Add rate limiting** to `/api/cid` — IP-based, 10-20 req/min per client
2. **Add React error boundaries** around Canvas and CID panel
3. **Add ARIA labels** to all icon-only buttons (quick accessibility win)
4. **Split useStore.ts** into domain-specific sub-stores (graph, CID, UI)
5. **Add unit tests** for `topoSort`, `inferEdgeLabel`, `analyzeIntent` — pure functions, easy to test
6. **Remove deprecated `apiKey`** field from NodeData type

## Dependency Health

All dependencies current. No deprecated or vulnerable packages detected.
- Next.js 16.1.6, React 19.2.4, Zustand 5.0.11, React Flow 12.10.1
- TypeScript 5.9.3, ESLint 9.39.4, Tailwind CSS 4.2.1
